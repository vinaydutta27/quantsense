const {chromium}=require('/Users/vinaydutta/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const assert=require('assert');
(async()=>{const b=await chromium.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true}),p=await b.newPage(),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.route('https://www.googletagmanager.com/**',r=>r.abort());for(const file of ['yield','liquidity']){await p.goto('http://127.0.0.1:8770/'+file+'.html');const lessons=await p.evaluate(()=>TREASURY_LESSONS.map(l=>({id:l.id,inputs:l.inputs})));assert.equal(lessons.length,7);for(const width of [390,768,1440]){await p.setViewportSize({width,height:1000});for(const l of lessons){await p.evaluate(id=>TREASURY_APP.configure({lesson:id}),l.id);assert.equal(await p.locator('.concept-questions li').count(),10);for(const d of l.inputs){await p.locator('#f-'+d.id).fill(String(d.max));await p.locator('#f-'+d.id).dispatchEvent('input');}assert(!/NaN|undefined|Infinity/.test(await p.locator('#stats').innerText()),l.id);assert(!(await p.evaluate(()=>document.documentElement.scrollWidth>innerWidth+1)),file+' '+l.id+' '+width);await p.locator('#reset').click();assert.deepEqual(await p.evaluate(()=>TREASURY_APP.getState().values),Object.fromEntries(l.inputs.map(d=>[d.id,d.value])));}}await p.evaluate(()=>TREASURY_APP.configure({lesson:TREASURY_LESSONS[0].id}));await p.evaluate(()=>scrollTo(0,0));await p.screenshot({path:'/private/tmp/'+file+'-lab.png'});}assert.deepEqual(errors,[]);await b.close();console.log('PASS: 14 applets, 140 questions, sliders and resets at three viewport sizes.');})().catch(e=>{console.error(e);process.exit(1)});
