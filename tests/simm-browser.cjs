const {chromium}=require('/Users/vinaydutta/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict'),fs=require('fs');
(async()=>{const b=await chromium.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true}),p=await b.newPage({viewport:{width:1440,height:1000}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.route('https://www.googletagmanager.com/**',r=>r.abort());const root=process.env.QUANTSENSE_TEST_URL||'http://127.0.0.1:8770';assert((await p.goto(root+'/simm.html')).ok());if(await p.getByRole('button',{name:'Decline',exact:true}).isVisible())await p.getByRole('button',{name:'Decline',exact:true}).click();const lessons=await p.evaluate(()=>SIMM_LESSONS.map(l=>({id:l.id,inputs:l.inputs})));assert.equal(lessons.length,29);
 for(const width of [390,768,1440]){await p.setViewportSize({width,height:1000});for(const l of lessons){await p.evaluate(id=>SIMM_APP.configure({lesson:id}),l.id);assert.equal(await p.locator('.concept-questions li').count(),10);for(const d of l.inputs){const el=p.locator('#f-'+d.id);if(d.options)await el.selectOption(d.options.at(-1)[0]);else{await el.fill(String(d.max));await el.dispatchEvent('input');}}assert(!/NaN|undefined|Infinity/.test(await p.locator('#stats').innerText()),l.id);assert(!/NaN|undefined|Infinity/.test(await p.locator('#chart').innerHTML()),l.id);assert(!(await p.evaluate(()=>document.documentElement.scrollWidth>innerWidth+1)),l.id+' overflow '+width);await p.locator('#reset').click();assert.deepEqual(await p.evaluate(()=>SIMM_APP.getState().values),Object.fromEntries(l.inputs.map(d=>[d.id,d.value])));await p.locator('.concept-questions summary').first().click();assert(await p.locator('.suggested-answer').first().isVisible());}}
 await p.goto(root+'/simm.html#curvature');assert.equal(await p.evaluate(()=>SIMM_APP.getState().lesson),'curvature');await p.locator('#next').click();assert.equal(await p.evaluate(()=>SIMM_APP.getState().lesson),'basis');await p.locator('.simm-jump a[href="#crif"]').click();await p.waitForFunction(()=>SIMM_APP.getState().lesson==='crif');await p.locator('#f-error').selectOption('units');assert((await p.locator('#explanation').innerText()).includes('units'));
 const [download]=await Promise.all([p.waitForEvent('download'),p.locator('#simm-export').click()]);const csv=fs.readFileSync(await download.path(),'utf8');assert(csv.length>500);assert(!/NaN|undefined|Infinity/.test(csv));
 fs.mkdirSync('/private/tmp/quantsense-simm',{recursive:true});for(const id of ['overview','concentration','curvature','total','crif']){await p.evaluate(id=>SIMM_APP.configure({lesson:id}),id);await p.evaluate(()=>scrollTo(0,0));await p.screenshot({path:'/private/tmp/quantsense-simm/'+id+'.png'});}await p.setViewportSize({width:390,height:900});await p.locator('.workspace').scrollIntoViewIfNeeded();await p.screenshot({path:'/private/tmp/quantsense-simm/mobile.png'});assert.deepEqual(errors,[]);await b.close();console.log('PASS: 29 concepts at three widths, all controls, resets, questions, deep links, data error and CSV export.');})().catch(e=>{console.error(e);process.exit(1)});
